{% extends 'base.html.twig' %}
{% block title 'Tous les biens'%}


{% block body %}
    {% if current_menu=='proprtties' %}
    <div class="container mt-4">
        <h1>Liste des biens à vendre</h1>
    </div>
        <div class="container mt-4">
            <div class="row">

                {#<div class="col-md-4">#}
                    {#<div class="jumbotron ">#}
                        {#<h4>{{ advert.title }}</h4>#}
                        {#<p>{{ advert.description }}</p>#}
                        {#<price>Prix: {{ advert.price }}</price><br>#}
                        {#<adress>Adresse: {{ advert.adresse }}</adress>#}
                    {#</div>#}
                {#</div>#}
            </div>
        </div>
    {% endif %}
    {% if current_menu=='pret' %}
        <div class="container mt-4">
            <h1>Liste des biens à prêt</h1>
        </div>
    {% endif %}
{% endblock %}